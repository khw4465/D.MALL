<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.dao.ProdOptMapper">

    <!-- select -->
    <select id="selectProdOpt" >
        SELECT * FROM prod_opt WHERE opt_cd = #{optCd}
    </select>

    <!-- insert -->
    <insert id="insertProdOpt" >
        INSERT INTO prod_opt (opt_cd, prod_cd, opt_name, opt_qty, opt_type, sale_prc, useYn, del_yn, remark, fstReg, fstRegr, lastUpd, lastUpdr)
        VALUES (#{optCd}, #{prodCd}, #{optName}, #{optQty}, #{optType}, #{salePrc}, #{useYn}, #{delYn}, #{remark}, #{fstReg}, #{fstRegr}, #{lastUpd}, #{lastUpdr})
    </insert>

    <!-- update -->
    <update id="updateProdOpt" >
        UPDATE prod_opt SET
                            prod_cd = #{prodCd},
                            opt_name = #{optName},
                            opt_qty = #{optQty},
                            opt_type = #{optType},
                            sale_prc = #{salePrc},
                            useYn = #{useYn},
                            del_yn = #{delYn},
                            remark = #{remark},
                            fstReg = #{fstReg},
                            fstRegr = #{fstRegr},
                            lastUpd = #{lastUpd},
                            lastUpdr = #{lastUpdr}
        WHERE opt_cd = #{optCd}
    </update>

    <!-- delete -->
    <delete id="deleteProdOpt" >
        DELETE FROM prod_opt WHERE opt_cd = #{optCd}
    </delete>

    <!-- delete all -->
    <delete id="deleteAllProdOpt">
        DELETE FROM prod_opt
    </delete>

    <!-- select all -->
    <select id="selectAllProdOpt" >
        SELECT * FROM prod_opt
    </select>

    <!-- count -->
    <select id="countProdOpt" >
        SELECT COUNT(*) FROM prod_opt
    </select>

</mapper>